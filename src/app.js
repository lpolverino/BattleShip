import { createGame } from "./game"

const createApp = () =>{

    let game = createGame

    while(!game.hasWinner()){
        // wait player / computer attack
    }
    return{

    }
}



module.exports = {
    createApp
}

